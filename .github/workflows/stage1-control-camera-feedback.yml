name: Stage 1 Adaptive Joystick Camera Regression

on:
  push:
    branches:
      - worldlife-reference-docs
    paths:
      - ".github/workflows/stage1-control-camera-feedback.yml"
      - "probes/android_stage1/scripts/probe_world.gd"
      - "probes/android_stage1/scenes/probe_world.tscn"
      - "probes/android_stage1/tests/world_boundary_preflight.py"
      - "probes/android_stage1/docs/CONTROL_CAMERA_FOUNDATION_README.md"
      - "ci/stage1/control_camera_phone_feedback_test.gd"

permissions:
  contents: read

jobs:
  control-camera-regression:
    name: Verify adaptive steering, larger arena, first-person damping, and FOV
    runs-on: ubuntu-latest
    timeout-minutes: 5

    steps:
      - name: Checkout tested revision
        uses: actions/checkout@v4

      - name: Set up Godot 4.7.2
        uses: chickensoft-games/setup-godot@v2
        with:
          version: 4.7.2
          use-dotnet: false
          include-templates: false

      - name: Static enlarged-arena boundary regression
        shell: bash
        run: |
          set -euo pipefail
          python3 probes/android_stage1/tests/world_boundary_preflight.py

      - name: Godot import and parse gate
        shell: bash
        run: |
          set -euo pipefail
          mkdir -p build-evidence
          godot --headless --editor --path probes/android_stage1 --quit 2>&1 | tee build-evidence/control-camera-import.log
          if grep -Eiq "SCRIPT ERROR|Parse Error|Failed loading resource|Cannot open file|Node not found" build-evidence/control-camera-import.log; then
            exit 1
          fi

      - name: Run adaptive joystick regression
        shell: bash
        run: |
          set -euo pipefail
          godot --headless --path probes/android_stage1 --script "$GITHUB_WORKSPACE/ci/stage1/control_camera_phone_feedback_test.gd" 2>&1 | tee build-evidence/control-camera-phone-feedback.log
          grep -q "Gate: STAGE1_ADAPTIVE_JOYSTICK_RUNTIME_VERIFIED" build-evidence/control-camera-phone-feedback.log
